# FontShooser
a React component that allows the reader of a web page to change the appearance of a piece of text.  When the user clicks on the text, a small form will appear that contains
Download the files chooser.html ant FontShooser.js,
open a terminale and cd to the directory where the files are, and tape this command
 python3 -m http.server

then open the browser and use this adress
http://localhost:8000/chooser.html
